[package]
name = "rust_executor"
version = "0.1.0"
edition = "2021"

[lib]
name = "rust_executor"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.20.0", features = ["extension-module"] }
